import React from "react";
import PropTypes from "prop-types";

import "./Navbar.scss";

const Navbar = () => {
    return (
        <div className="navbar">
            navbar
        </div>
    );
};

Navbar.propTypes = {};

export default Navbar;