import React from "react";
import PropTypes from "prop-types";

import "./Footer.scss";

const Footer = () => {
    return (
        <div className="footer">
            footer
        </div>
    );
};

Footer.propTypes = {};

export default Footer;