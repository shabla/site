import React from "react";
import PropTypes from "prop-types";

import "./Sidebar.scss";

const Sidebar = () => {
    return (
        <div className="sidebar">
            sidebar
        </div>
    );
};

Sidebar.propTypes = {};

export default Sidebar;